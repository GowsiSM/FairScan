# Copy this to .env.local and fill in values

# Backend URL (leave as-is for local dev)
VITE_API_URL=http://localhost:8000

# Set to "true" to run frontend without a backend (uses mock data)
VITE_MOCK=false
